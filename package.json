{
  "name": "freesync",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/server",
    "packages/plugin",
    "packages/FreeSyncApp",
    "packages/web"
  ],
  "scripts": {
    "test:sync": "bash scripts/test-sync.sh"
  },
  "engines": {
    "node": ">=20"
  }
}
