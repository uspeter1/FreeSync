{
  "name": "@freesync/plugin",
  "version": "0.0.0",
  "description": "FreeSync Obsidian plugin — real-time collaborative sync with live presence",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch"
  }
}
